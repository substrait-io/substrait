---
# Example: extension defining user-defined types (point, line). Used throughout the
# documentation to illustrate referencing user-defined types across extensions.
urn: extension:example:extension_types
types:
  - name: point
    structure:
      latitude: i32
      longitude: i32
  - name: line
    structure:
      start: u!point
      end: u!point
